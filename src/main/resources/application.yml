spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/coderank
    username: coderank
    password: coderank
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
        format_sql: true
    open-in-view: false
coderank:
  jwt:
    secret: change-me-to-a-real-secret-key-at-least-32-bytes-long-please-ok
    expiration-ms: 86400000   # 24 hours

